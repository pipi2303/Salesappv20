node_modules/
dist/
.env
.env.local
.env.production
.vercel

# Local-disk backend for check-in photos (lib/blob.ts), used when
# BLOB_READ_WRITE_TOKEN isn't set -- never committed, persisted via the
# checkin_photos Docker volume in production instead.
/uploads/
